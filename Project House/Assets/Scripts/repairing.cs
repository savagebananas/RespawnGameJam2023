using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class repairing : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Make list of repairs
    }

    // Update is called once per frame
    void Update()
    {
        //if at place to repair, stop for 5 seconds and then remove task
        //if colides with phone ring, then set destination to phone location
    }

    //public void remove from tasks
}
