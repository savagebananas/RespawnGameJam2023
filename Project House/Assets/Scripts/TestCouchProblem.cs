using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestCouchProblem : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       // if (Mathf.Abs(Time.time-10)<0.1) Debug.Log("Couch Testx : " + GameObject.Find("Couch (1)").GetComponent<>colliders.Count);

    }
}
